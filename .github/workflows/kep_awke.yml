name: Kep Awke now

on:
  schedule:
    # This runs every 15 minutes
    - cron: '*/15 * * * *'
  workflow_dispatch: # Allows you to start it manually for testing

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visit Space
        run: |
          # Replace the URL below with your actual Hugging Face Space URL
          curl -I https://a334f-model-inference-check.hf.space
